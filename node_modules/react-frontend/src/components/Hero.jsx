/**
 * Full-viewport hero with headline and subtitle.
 * @returns {import('react').JSX.Element}
 */
export default function Hero() {
  return (
    <section className="snap-section hero-block">
      <h1 className="hero-title">TLDR.</h1>
      <p className="hero-subtitle">AI-curated news, summarized.</p>
    </section>
  )
}
